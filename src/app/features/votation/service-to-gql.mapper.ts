import {VotationFormGroupData} from "./votation-form-group.service";
import {VotationRef} from "../../../graphql/generated/graphql";

export function votationInput(vote:VotationFormGroupData):VotationRef {
    return {

    }
}
