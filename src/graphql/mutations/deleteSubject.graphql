mutation DeleteSubject($subjectId: [ID!], $subjectId2: ID!) {
  deleteSubject(filter: { id: $subjectId }) {
    subject {
      id
    }
  }
  updateFederal_Subjects(input: { filter: { id: "0x1a5193fcb7" }, remove: { subjects: { id: $subjectId2 } } }) {
    federal_Subjects {
      id
    }
  }
}
